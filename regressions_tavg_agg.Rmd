## Linear regressions: relative [protein] vs. mean annual temp (oC)

# Summary stats for linear models
```{r, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}
source('scripts/transformations.R')
library(knitr)
source('scripts/prep_data.R')

tab <- regression_agg(data, 'tavg', logx=FALSE)

kable(tab, "markdown", align="l", padding=10)
write_csv(tab, 'output/modelorder.csv')

modelorder <- read_csv('output/modelorder.csv')
````

# Linear regressions

```{r, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}

for(i in modelorder$submodel) {
  
  agg_plot(data, depvar = i, indepvar = 'tavg', logx=FALSE, labs = 'Mean annual temp (oC)')
  
}

````
