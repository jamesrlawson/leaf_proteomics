## Linear regressions: relative [protein] vs. irradiance at leaf (scaled by gap fraction)

```{r, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}
source('scripts/transformations.R')
require(ggplot2)
require(vegan)
library(readr)
library(knitr)
require(plyr)
require(reshape2)
require(dplyr)
require(lazyeval)

source('scripts/prep_data_.R')

tab <- regression_agg(data, 'leafrad_mean', logx=TRUE)

#kable(tab, "markdown", align="l", padding=10)
write_csv(tab, 'output/modelorder.csv')

modelorder <- read_csv('output/modelorder.csv')
````


```{r, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}

for(i in modelorder$submodel) {
  
  agg_plot_leafrad(data, depvar = i, indepvar = 'leafrad_mean', logx=TRUE, labs = 'irradiance at leaf')
  
}

````
