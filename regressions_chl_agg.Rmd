## Linear regressions: total [protein] vs. total chlorophyll (umol/m2)

# Summary stats for linear models
```{r, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}
include_chlorophyll = TRUE

source('scripts/transformations.R')
source('scripts/prep_data_mg_per_mm2.R')
library(knitr)

tab <- regression_agg(data, 'mg_Cl_total_per_m2', logx=TRUE)

#kable(tab, "markdown", align="l", padding=10)
write_csv(tab, 'output/modelorder.csv')

modelorder <- read_csv('output/modelorder.csv')
````

# Linear regressions

```{r, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}

for(i in modelorder$submodel) {
  
  agg_plot(data, depvar = i, indepvar = 'mg_Cl_total_per_m2', logx=TRUE, labs = 'total chlorophyll (umol/m2)')
  
}

````
